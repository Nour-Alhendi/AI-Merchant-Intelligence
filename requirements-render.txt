# Dependencies for the FastAPI backend (api.py) deployed on Render.
# Local development uses environment.yml / conda instead — this file is
# scoped to the Render build only.

--extra-index-url https://download.pytorch.org/whl/cpu

numpy==1.26.4
pandas==2.3.3
python-dotenv
fastapi
uvicorn[standard]
pydantic

llama-index-core==0.14.23
llama-index-embeddings-huggingface==0.7.0
llama-index-llms-groq==0.5.0

sentence-transformers==5.6.0
torch==2.12.1+cpu
transformers==4.57.6
huggingface-hub==0.36.2